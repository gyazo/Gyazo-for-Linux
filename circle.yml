dependencies:
  pre:
    - gem install package_cloud

deployment:
  production:
    branch: master
    commands:
      - sh ./deb.sh
      - package_cloud push gyazo/gyazo-for-linux ../gyazo*all.deb || true
