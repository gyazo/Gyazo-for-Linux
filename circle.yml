machine:
  environment:
    PACKAGE: gyazo
    VERSION: 1.2

test:
  override:
    - ruby test/tc_gyazo_client.rb

deployment:
  production:
    branch: release
    commands:
      - sh ./deb.sh
      - ruby ./circle_release.rb
      - bash ./rpm_docker.sh
